{
  "devDependencies": {
    "@prettier/plugin-php": "^0.22.2",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5"
  },
  "scripts": {
    "prepare": "husky",
    "prettier": "prettier"
  },
  "lint-staged": {
    "src/app/**/*.php": "prettier --write",
    "src/config/**/*.php": "prettier --write"
  }
}
